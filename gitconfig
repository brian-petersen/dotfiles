[user]
	email = skiwalk3r@gmail.com
	name = Brian Petersen

[push]
	default = simple

[include]
	path = .gitconfig.local

[init]
	defaultBranch = main

# Potential things to include in the gitconfig.local
# [core]
	# For wsl set ups, this allows 1password
	# to be used to retrieve private keys securely
	# sshCommand = ssh.exe
